@model IEnumerable<CapaDatos.Modelo.contactoEntidadEjecutora_has_entidadEjecutora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.contactoEntidadEjecutora.tipoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.entidadEjecutora.nit)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.contactoEntidadEjecutora.tipoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.entidadEjecutora.nit)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
