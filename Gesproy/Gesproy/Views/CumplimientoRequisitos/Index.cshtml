@model IEnumerable<CapaDatos.Modelo.cumplimiento_requisitos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numero_doc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cumplimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lis_detalle.codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numero_doc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cumplimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lis_detalle.codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
